services:
  redis:
    image: redis:latest
    container_name: my-local-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    environment:
      - REDIS_ARGS=--appendonly yes
    restart: unless-stopped

volumes:
  redis_data:
    driver: local
